Напишите программу, которая выполняет два вычисления и выводит результаты на экран (каждый — в отдельной строке):

1. В одном байте 8 бит, каждый бит принимает одно из 2 состояний (0 или 1). Количество уникальных значений считается как 2 в степени числа бит. Сколько значений может хранить один байт?
2. Вы купили 9 пирожных на троих. Сколько достанется каждому?

Используйте оператор возведения в степень `**` и оператор деления `/`.
