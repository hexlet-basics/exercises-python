The application shows a short summary of a text: its first and last character. Print the first and the last character of the string from the variable `text` in the following format:

```text
First: H
Last: !
```

Try to create only one variable that immediately holds the required text before printing it to the screen. In this lesson we practise building a compound expression.
