Реализуйте функцию `count_hashtags()`, которая принимает текст публикации и считает, сколько раз в нем встречается символ `#`.

```python
count_hashtags('New post #python #code')  # 2
count_hashtags('No tags here')            # 0
count_hashtags('#start and #finish')      # 2
```
