Напишите функцию `mask_card_number()`, которая скрывает номер банковской карты. Функция должна заменить все символы строки на `*`, кроме последних четырех.

Пример работы:

```python
mask_card_number("1234567812345678")  # "************5678"
mask_card_number("12345678")          # "****5678"
```
