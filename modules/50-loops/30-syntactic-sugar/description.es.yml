---

name: Azúcar sintáctico
theory: |

  Construcciones como `index = index + 1` se utilizan frecuentemente en Python, por lo que los creadores del lenguaje agregaron una forma abreviada: `index += 1`.

  Solamente difieren en la forma de escribirlos. El intérprete convertirá la construcción abreviada en la expandida.

  Estas abreviaturas se llaman **azúcar sintáctico**, porque hacen que el proceso de escribir código sea un poco más fácil y agradable.

  Existen formas abreviadas para todas las operaciones aritméticas y para la concatenación de cadenas:

  * `a = a + 1` → `a += 1`
  * `a = a - 1` → `a -= 1`
  * `a = a * 2` → `a *= 2`
  * `a = a / 1` → `a /= 1`

instructions: |

  Implementa la función `build_progress_bar()`, que recibe la cantidad de pasos completados y la cantidad total de pasos, y devuelve una cadena que representa una barra de progreso.

  Los pasos completados se muestran con `#`, y los pasos restantes se muestran con `-`. Intenta no utilizar métodos incorporados para trabajar con cadenas en tu solución.

  ```python
  build_progress_bar(0, 5)  # '-----'
  build_progress_bar(3, 5)  # '###--'
  build_progress_bar(5, 5)  # '#####'
  ```

tips:
  - |
    [Azúcar sintáctico](https://es.wikipedia.org/wiki/Azúcar_sintáctico)

definitions:
  - name: Azúcar sintáctico
    description: |
      son características sintácticas que no afectan el comportamiento en sí del programa, pero hacen que el uso del lenguaje sea más conveniente para los humanos.
